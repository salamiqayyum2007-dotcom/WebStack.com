// (Deprecated) Firebase-based auth helpers
//
// Firebase support has been removed from the site. All auth flows are now handled
// via the local backend endpoints (`/api/login`, `/send-reset-code`, `/verify-reset-code`).

console.log('auth.js is no longer used.');

